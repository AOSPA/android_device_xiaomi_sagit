type display_count, domain;
type display_count_exec, exec_type, file_type;

# Allow for transition from init domain to display_count
init_daemon_domain(display_count)

allow display_count display_misc_file:dir create_dir_perms;
allow display_count display_misc_file:file create_file_perms;

allow display_count self:capability { dac_override dac_read_search };

allow display_count sysfs:file rw_file_perms;
allow display_count sysfs_graphics:file rw_file_perms;
